# API base URL (no trailing slash)
VITE_API_URL=http://localhost:3001

# Dev-mode user ID. The backend's _auth.ts accepts `Authorization: Dev <userId>`.
# Replace with a real auth flow in production.
VITE_DEV_USER_ID=507f1f77bcf86cd799439011
